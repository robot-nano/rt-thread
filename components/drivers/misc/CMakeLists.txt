include_directories(${CMAKE_CURRENT_SOURCE_DIR})

## driver misc
if (RT_USING_PIN)
	list(APPEND DRIVER_SRCS misc/pin.c)
endif ()
if (RT_USING_ADC)
	list(APPEND DRIVER_SRCS misc/adc.c)
endif ()
if (RT_USING_DAC)
	list(APPEND DRIVER_SRCS misc/dac.c)
endif ()
if (RT_USING_PWM)
	list(APPEND DRIVER_SRCS misc/rt_drv_pwm.c)
endif ()
if (RT_USING_PULSE_ENCODER)
	list(APPEND DRIVER_SRCS misc/pulse_encoder.c)
endif ()
if (RT_USING_INPUT_CAPTURE)
	list(APPEND DRIVER_SRCS misc/rt_inputcapture.c)
endif ()